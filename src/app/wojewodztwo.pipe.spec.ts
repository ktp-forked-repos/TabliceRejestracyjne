import { WojewodztwoPipe } from './wojewodztwo.pipe';

describe('WojewodztwoPipe', () => {
  it('create an instance', () => {
    const pipe = new WojewodztwoPipe();
    expect(pipe).toBeTruthy();
  });
});
